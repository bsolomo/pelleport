var should = require('should');

describe('Should', function() {
	describe('#indexOf()', function() {
		it('should return -1 when the value is not present', function() {
			[1,2,5].indexOf(5).should.equal(2);
			[1,2,0].indexOf(0).should.equal(2);
		    });
	    });
    });
